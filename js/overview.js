// overview.js

function renderOverview() {
  return UI.pageHeader({ title: 'Overview', subtitle: 'Platform summary and key metrics'})
    + _pageStub('Overview', 'Platform-wide reporting and KPIs will appear here.');
}
